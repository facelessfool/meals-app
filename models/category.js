// this will define how a category object in my application should look like
class Category {
  constructor(id, cuisine, color) {
    this.id = id;
    this.cuisine = cuisine;
    this.color = color;
  }
}

export default Category;
