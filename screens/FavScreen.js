import React from "react";
import { View, Text, Stylesheet, headerTitle } from "react-native";
// import {headerTitle} from 'react-navigation-header'

const FavScreen = (props) => {
  return <Text>FavScreen!</Text>;
};

FavScreen.navigationOptions = {
  headerTitle: "Your Favorites",
};

export default FavScreen;
